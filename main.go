package main

func main() {

	/*
		Controller.Insert(true, "dengzhichao", "female", "33333@163.com")

		result := Controller.Select(true, 2)
		fmt.Printf("找到%d条\n", len(result))
		for _, val := range result {
			fmt.Println(val)
		}
		Controller.Update(true, "woshipaidaxing", 2)
		Controller.Delete(true, 2)

		Controller.BatchDelete(true, 10, 11, 12, 0, 1, 2)
		slice := []int{17, 18, 19, 20}
		result := Controller.BatchSelect(true, slice...)
		fmt.Printf("找到%d条\n", len(result))
		for _, val := range result {
			fmt.Println(val)
		}
	*/

}
